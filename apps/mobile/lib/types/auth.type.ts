export type LoginType = {
  email: string;
  password: string;
  deviceId: string | null;
};

export type RegisterType = {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
};
